// Only these subjects have content being actively ingested right now —
// every other subject renders as a disabled "Coming Soon" card/page.
export const ACTIVE_SUBJECT_SLUGS = new Set(['history', 'environment', 'polity']);
